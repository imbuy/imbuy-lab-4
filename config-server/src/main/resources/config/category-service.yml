server:
  port: 0

spring:
  application:
    name: category-service
  r2dbc:
    url: r2dbc:postgresql://localhost:5434/category_db
    username: imbuy
    password: secret
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

springdoc:
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    imbuy.category: DEBUG
    org.flywaydb: INFO
    org.springframework.boot.autoconfigure.flyway: DEBUG

app:
  security:
    jwt:
      secret: ${JWT_SECRET:dGhpc19pcy1hLWxvbmdlci1iYXNlNjQtand0LXNlY3JldC1rZXk=}

